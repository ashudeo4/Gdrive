import { type Icon } from "@tabler/icons-react"

import {
  SidebarGroup,
} from "@/components/ui/sidebar"

export function NavMain({
}: {
  items: {
    title: string
    url: string
    icon?: Icon
  }[]
}) {
  return (
    <SidebarGroup>

    </SidebarGroup>
  )
}
